<h2><%= @subcategory.name %></h2>

<ul>
	<% @subcategory.items.each do |item| %>
	<li>
		<ul>
			<li>
				<%= link_to(image_tag(item.image), item) %>
			</li>
			<li>
				<%= item.name %>
			</li>
		</ul>	 
	</li>
	<% end %>
</ul>
