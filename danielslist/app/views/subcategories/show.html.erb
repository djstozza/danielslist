<div class="jumbotron">
	<h2><%= @subcategory.name %></h2>

	<ul>
		<% @subcategory.items.each do |item| %>
		<li>
			<ul>
				<div class="col-md-2">
					<li>
						<%= link_to(cl_image_tag(item.image, :width=>200, :crop=>:thumb, :gravity=>:face, :effect=>"vibrance"), item) %>
					</li>
					<li>
						<%= item.name %>
					</li>
				</div>
				
			</ul>	 
		</li>
	<% end %>
</ul>
</div>


